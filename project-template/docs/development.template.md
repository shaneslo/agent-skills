# Development — how we dev {{PROJECT_NAME}}

The local loop: get it running, make a change, see the change. If a new engineer
can't get to a running system from this doc alone, the doc is wrong — fix it.

## Prerequisites

<!-- FILL: exact versions. "Node 20", "Python 3.12", "Docker". Pin them — "latest"
     is how environments drift. Link to a version manager (nvm/pyenv/asdf) if used. -->

- 
- 

## First-time setup

```bash
# FILL: clone → install → any bootstrap (env file, db migrate, seed data).
# Every line runnable on a clean checkout. If a step needs a secret, say where
# it comes from — never inline it.
```

## The dev loop

```bash
# FILL: the command(s) you run all day.
# run:
# watch/hot-reload:
# lint + format:
```

## Project layout

<!-- FILL: the 5-10 directories that matter and what lives in each. Point people
     at where to start reading. Mirror the components in ARCHITECTURE.md. -->

| Path | Contains |
| --- | --- |
| | |

## Debugging

<!-- FILL: how to attach a debugger / read logs / reproduce locally. Any gotchas
     that cost you an afternoon — write them here so nobody pays that cost twice. -->

## Common tasks

<!-- FILL: the recurring chores — add a migration, regenerate a client, update a
     fixture, bump a dependency. One command each. -->

- **<task>:** `<command>`

## Environment variables / config

<!-- FILL: the vars needed to run locally, with example (non-secret) values.
     Keep this in sync with a committed `.env.example`. -->

| Var | Purpose | Example |
| --- | --- | --- |
| | | |
